var msg;
msg="<p><code>The script is located in the external script file called math.js</code></p>";
function addNumbers(headVar,bodyVar)
{
    document.write(msg);
    document.writeln("Addition of "+headVar+" and "+bodyVar+" is "+(headVar+bodyVar));
}